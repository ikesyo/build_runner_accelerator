name: current_json_app
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  json_annotation: 4.12.0

dev_dependencies:
  build_runner: 2.16.1
  fast_build_runner_worker:
    path: ../../dart_worker
  json_serializable: 6.14.1
