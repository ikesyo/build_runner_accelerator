name: applies_builder_app
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  build: 4.0.10

dev_dependencies:
  build_runner: 2.16.1
  build_runner_accelerator_worker:
    path: ../../dart_worker
