name: json_serializable_app
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  # build_resolvers 3.0.4 currently brings build_runner_core 9.3.2,
  # which pins build_runner to 2.7.2.
  build_runner: 2.7.2
  json_serializable: 6.11.2
  fast_build_runner_worker:
    path: ../../dart_worker
