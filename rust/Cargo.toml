[package]
name = "build_runner_accelerator"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
notify = "8.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
